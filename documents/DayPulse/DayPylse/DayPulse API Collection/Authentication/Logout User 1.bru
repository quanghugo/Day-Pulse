meta {
  name: Logout User 1
  type: http
  seq: 7
}

post {
  url: {{base_url}}/auth/logout
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "token": "{{user1_access_token}}"
  }
}

script:post-response {
  test('User 1 logged out', () => {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
